# SET UP COMMAND FOR TERMUX

```set up
cd
pkg update -y && pkg upgrade -y
pkg install nodejs python golang chromium tur-repo wget curl git -y
pip3 install colorama pyfiglet
npm install puppeteer-core
git clone https://github.com/ELLIOTpxp/FSY-V1.git
cd FSY-V1
chmod +x FSY.py quantum_hulk
python3 FSY.py
```
